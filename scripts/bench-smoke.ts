/**
 * `pnpm bench:smoke` — 5 cheap eval tasks, run in CI on every PR (SPEC §4.9).
 *
 * STUB (F4): the eval harness lands in F10. This exits 0 so the CI shape is
 * established now, and reports honestly that nothing was measured.
 */

function main(): number {
  console.log("bench:smoke — SPEC §4.9");
  console.log("  status: NOT YET IMPLEMENTED (stub; eval harness lands in F10)");
  return 0;
}

process.exit(main());
